const userName = prompt('Enter first name');
const userLastName = prompt('Enter last name');
const userAge = prompt('Enter your age');

alert(`Hi, ${userName} ${userLastName}, your age - ${userAge}`);
